target/
node_modules/
__pycache__/
*.pyc
.venv/
build/
dist/

# Real secrets — every app's own README/.env.*.example documents what
# goes in these. Never commit the filled-in version.
.env
.env.*
!.env.example
!.env.*.example

# Unrelated payments-admin template kept locally only as a UI reference,
# never wired to this backend — not part of the realtime platform.
saas-admin/

# Local SQLite dev databases (backend/portal.db and friends) — never the
# real thing to commit, same reasoning as .env above.
*.db
*.db-shm
*.db-wal
