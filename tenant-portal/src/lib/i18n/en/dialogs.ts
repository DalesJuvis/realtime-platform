/** Generic dialog chrome (`DialogProvider`'s own close button) and
 * `ConfirmDialog`'s default button labels — page-specific confirm
 * messages/titles stay in each page's own module. */
export const dialogs = {
  closeAriaLabel: 'Close dialog',
  defaultConfirmLabel: 'Confirm',
  defaultCancelLabel: 'Cancel',
}
